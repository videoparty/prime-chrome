/**
 * Get displayname.
 */
getDisplayName();

/**
 * Get current party.
 * When there is no party,
 * a new one will be created.
 */
getCurrentParty();

/**
 * Setup the window message listener
 */
setupWindowListener();